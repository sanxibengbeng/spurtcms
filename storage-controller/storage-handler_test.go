package storagecontroller

import (
	"testing"
)

func TestUploadImage(t *testing.T) {
	t.Run("Local storage", func(t *testing.T) {
		// Skip test for now
		t.Skip("Skipping test until proper mocking is implemented")
	})

	t.Run("S3 storage", func(t *testing.T) {
		// Skip test for now
		t.Skip("Skipping test until proper mocking is implemented")
	})

	t.Run("Azure storage", func(t *testing.T) {
		// Skip test for now
		t.Skip("Skipping test until proper mocking is implemented")
	})
}
