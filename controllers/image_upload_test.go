package controllers

import (
	"testing"
)

func TestImageUpload(t *testing.T) {
	t.Run("Upload base64 image", func(t *testing.T) {
		// Skip test for now
		t.Skip("Skipping test until proper mocking is implemented")
	})

	t.Run("Image resize", func(t *testing.T) {
		// Skip test for now
		t.Skip("Skipping test until proper mocking is implemented")
	})
}
