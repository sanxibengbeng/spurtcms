package models

import (
	"testing"
)

func TestStorage(t *testing.T) {
	t.Run("Get storage value", func(t *testing.T) {
		// Skip test for now
		t.Skip("Skipping test until proper mocking is implemented")
	})

	t.Run("Update storage config", func(t *testing.T) {
		// Skip test for now
		t.Skip("Skipping test until proper mocking is implemented")
	})
}
